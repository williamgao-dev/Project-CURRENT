namespace Project.Core
{
    public enum MenuItemType
    {
        /// <summary>
        /// Shows the menu text and an icon
        /// </summary>
        TextAndIcon = 0,
        Divider = 1,
        Header = 2,
    }
}
